training:
  task: masked_language_modeling
  batch_size: 32
  max_steps: 500
  learning_rate: 3.0e-4
  weight_decay: 0.01
  seed: 42
  log_every: 50
  num_workers: 0
  pin_memory: true

masking:
  token_mask_prob: 0.15
